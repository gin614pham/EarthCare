export interface Location {
  address: string;
  description: string;
  image: string[];
  locationType: string;
  longitude: number;
  latitude: number;
}

export interface CarouselItems {
  title: string;
  icon: string;
}
